<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drazard.dndmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".NewCampaignActivity" />
        <activity android:name=".EditCampaignActivity" />
        <activity
            android:name=".CharacterRaceSelectionActivity"
            android:label="@string/activity_title_character_race"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity android:name=".CharacterClassSelectionActivity" />
        <activity android:name=".CharacterBackgroundSelectionActivity" />
    </application>

</manifest>